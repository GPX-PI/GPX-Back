name: "SonarCloud Setup Helper"
description: "Ayuda a configurar un proyecto para SonarCloud desde cero"

steps:
  - name: "Verificar tipo de proyecto"
    command: |
      echo "🔍 Detectando tipo de proyecto..."
      
      if (Test-Path "pom.xml") {
        echo "✅ Proyecto Maven detectado"
        $projectType = "maven"
      } elseif (Test-Path "build.gradle" -or Test-Path "build.gradle.kts") {
        echo "✅ Proyecto Gradle detectado"
        $projectType = "gradle"
      } elseif (Test-Path "package.json") {
        echo "✅ Proyecto Node.js/JavaScript detectado"
        $projectType = "nodejs"
      } else {
        echo "❌ Tipo de proyecto no reconocido"
        echo "💡 Este workflow está optimizado para Maven, Gradle o Node.js"
        exit 1
      }
      
      echo "🏷️ Tipo de proyecto: $projectType"

  - name: "Verificar configuración existente"
    command: |
      echo "🗺️ Verificando configuración actual..."
      
      if (Test-Path "sonar-project.properties") {
        echo "✅ Ya existe sonar-project.properties"
        echo "📝 Contenido actual:"
        Get-Content "sonar-project.properties" | Where-Object { $_ -match "sonar\.(organization|projectKey|projectName)" }
      } else {
        echo "⚠️ No existe sonar-project.properties"
        echo "📝 Necesitarás crear este archivo"
      }
      
      if (Test-Path "pom.xml") {
        $jacocoPlugin = Select-String -Path "pom.xml" -Pattern "jacoco-maven-plugin" -Quiet
        $sonarPlugin = Select-String -Path "pom.xml" -Pattern "sonar-maven-plugin" -Quiet
        
        if ($jacocoPlugin) {
          echo "✅ Plugin JaCoCo encontrado en pom.xml"
        } else {
          echo "⚠️ Plugin JaCoCo no encontrado en pom.xml"
        }
        
        if ($sonarPlugin) {
          echo "✅ Plugin SonarCloud encontrado en pom.xml"
        } else {
          echo "⚠️ Plugin SonarCloud no encontrado en pom.xml"
        }
      }

  - name: "Proporcionar guía de configuración"
    command: |
      echo ""
      echo "📋 ===== GUÍA DE CONFIGURACIÓN ====="
      echo ""
      
      if (-not (Test-Path "sonar-project.properties")) {
        echo "📄 1. Crear sonar-project.properties:"
        echo "   sonar.organization=TU_ORGANIZACION"
        echo "   sonar.projectKey=TU_PROYECTO_KEY"
        echo "   sonar.projectName=Nombre de tu proyecto"
        echo "   sonar.projectVersion=1.0"
        echo "   sonar.sources=src/main/java"
        echo "   sonar.tests=src/test/java"
        echo "   sonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml"
        echo ""
      }
      
      echo "🔑 2. Configurar token de SonarCloud:"
      echo "   - Ve a https://sonarcloud.io/account/security/"
      echo "   - Genera un nuevo token"
      echo "   - Ejecuta: `$env:SONAR_TOKEN='tu_token_aqui'`"
      echo ""
      
      if (Test-Path "pom.xml") {
        echo "⚙️ 3. Para proyectos Maven, asegúrate de tener estos plugins en pom.xml:"
        echo "   - jacoco-maven-plugin (para cobertura)"
        echo "   - sonar-maven-plugin (para análisis)"
        echo ""
      }
      
      echo "🚀 4. Una vez configurado, ejecuta:"
      echo "   warp-cli workflow run sonarcloud-coverage"
      echo ""
      echo "📚 Para más información: https://docs.sonarcloud.io/"

  - name: "Probar conexión básica"
    command: |
      echo "🌐 Probando conectividad con SonarCloud..."
      
      try {
        $response = Invoke-WebRequest -Uri "https://sonarcloud.io/api/system/status" -Method GET -TimeoutSec 10
        if ($response.StatusCode -eq 200) {
          echo "✅ SonarCloud es accesible"
        }
      } catch {
        echo "⚠️ No se pudo conectar a SonarCloud"
        echo "   Verifica tu conexión a internet"
      }
      
      if ($env:SONAR_TOKEN) {
        echo "✅ Variable SONAR_TOKEN está definida"
      } else {
        echo "⚠️ Variable SONAR_TOKEN no está definida"
        echo "   Recuerda configurarla antes de ejecutar el análisis"
      }

